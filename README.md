# 📊 LBKI CSV — мощный инструмент для работы с CSV-файлами

**LBKI CSV** — это простой, но функциональный инструмент на чистом Python для обработки CSV-файлов. Подходит для анализа данных, очистки, конвертации и автоматизации рутинных задач — без лишних зависимостей и с минималистичным подходом.

> Создано как альпинистское снаряжение: лёгко, надёжно, работает в любых условиях.

---

## 🔧 Возможности

- ✅ **Автоопределение кодировки**: UTF-8 или CP1251
- ✅ **Угадывание разделителя**: запятая, точка с запятой, табуляция
- ✅ **Подсчёт строк и просмотр данных**
- ✅ **Поиск по содержимому**
- ✅ **Фильтрация столбцов**
- ✅ **Смена кодировки** (UTF-8 ↔ CP1251)
- ✅ **Удаление дубликатов**
- ✅ **Группировка по столбцу** (сводная таблица)
- ✅ **Разделение файла на части + упаковка в ZIP**
- 🖼️ **Графический интерфейс (GUI)** — без установки дополнительных пакетов (`tkinter` встроен)

---

## 📋 Требования

- **Python 3.6+**
- Встроенные модули: `csv`, `zipfile`, `tkinter` (для GUI)
- **Никаких внешних зависимостей!**

---

## 🚀 Быстрый старт

### Консольная версия

```bash
python lbki_csv_cli.py ваш_файл.csv
```

**Пример:**
```bash
python lbki_csv_cli.py test_data.csv
```

**Вывод:**
```
 Обрабатываю: test_data.csv
 Кодировка: utf-8
Разделитель: ';'
✅ Загружено: 5 столбцов, 20 строк.

==================================================
МЕНЮ:
1. Подсчитать количество строк
2. Вывести первые N строк
3. Найти строки по заданной строке
4. Создать CSV с выбранными столбцами
5. Сменить кодировку файла
6. Удалить дубли строк
7. Разделить файл и упаковать в ZIP
8. Выйти

Выберите действие (1-8): 
```

### Графическая версия (GUI)

```bash
python lbki_csv_gui.py
```

**Интерфейс:**
- 📂 Кнопка для выбора CSV файла
- 📋 Список доступных функций
- ▶ Кнопка для запуска выбранных операций
- 💾 Встроенное сохранение результатов

---

## 📖 Подробное описание функций

### 1️⃣ Подсчитать количество строк
Выводит общее количество строк данных и столбцов в файле.

**Консоль:**
```
Выберите действие (1-8): 1

Количество строк данных: 20
Количество столбцов: 5
```

**GUI:** Показывает информационное окно с количеством строк и столбцов.

---

### 2️⃣ Вывести первые N строк
Показывает первые N строк файла для быстрого просмотра.

**Консоль:**
```
Выберите действие (1-8): 2
Сколько строк показать? 3

Имя	Город	Тип	Возраст	Статус
Алексей	Москва	Пеший	28	Активен
Борис	СПб	Водный	35	Ожидание
Виктор	Москва	Пеший	41	Активен
```

**GUI:** Открывает новое окно с таблицей данных.

---

### 3️⃣ Найти строки по заданной строке
Поиск по всем столбцам (регистронезависимый).

**Консоль:**
```
Выберите действие (1-8): 3
Введите строку для поиска: Москва

Найдено: 7
Имя	Город	Тип	Возраст	Статус
Алексей	Москва	Пеший	28	Активен
Виктор	Москва	Пеший	41	Активен
Фёдор	Москва	Водный	45	Активен
...
```

**GUI:** Запрашивает поисковый запрос в диалоговом окне и показывает результаты.

---

### 4️⃣ Создать CSV с выбранными столбцами
Фильтрует данные, оставляя только нужные столбцы.

**Консоль:**
```
Выберите действие (1-8): 4

Доступные столбцы: Имя, Город, Тип, Возраст, Статус
Столбцы через запятую: Имя, Город, Статус
Имя выходного файла (.csv): filtered_data.csv
Сохранено: filtered_data.csv
```

**GUI:** Показывает список столбцов с номерами, запрашивает номера для выбора.

**Результат (filtered_data.csv):**
```
Имя,Город,Статус
Алексей,Москва,Активен
Борис,СПб,Ожидание
...
```

---

### 5️⃣ Сменить кодировку файла
Конвертирует файл между UTF-8 и CP1251.

**Консоль:**
```
Выберите действие (1-8): 5
В какую кодировку перевести? (utf-8 / cp1251): cp1251
Имя нового файла: data_cp1251.csv
Кодировка изменена: utf-8 → cp1251
Файл сохранён: data_cp1251.csv
```

**GUI:** Запрашивает целевую кодировку и место сохранения файла.

---

### 6️⃣ Удалить дубли строк
Удаляет полные дубликаты строк, сохраняя первое вхождение.

**Консоль:**
```
Выберите действие (1-8): 6

Удалено дублей: 2
Осталось строк: 18
```

**GUI:** Показывает количество удалённых дубликатов и обновляет данные в памяти.

---

### 7️⃣ Свод по столбцу (только GUI)
Группирует данные по выбранному столбцу и подсчитывает количество значений.

**GUI:**
```
Свод по 'Город'
Значение        Количество
Москва          5
СПб             4
Казань          3
Екатеринбург    3
Новосибирск     2
Мурманск        1
```

Результаты можно сохранить в CSV файл.

---

### 8️⃣ Разделить файл и упаковать в ZIP
Разбивает большой файл на части и упаковывает в архив.

**Консоль:**
```
Выберите действие (1-8): 7
По сколько строк в части? 5
Базовое имя частей: data
Имя ZIP-архива (без .zip): data_split

Добавлен в архив: data_1.csv (5 строк)
Добавлен в архив: data_2.csv (5 строк)
Добавлен в архив: data_3.csv (5 строк)
Добавлен в архив: data_4.csv (5 строк)

✅ Все части упакованы в: data_split.zip
```

**GUI:** Запрашивает размер части, базовое имя и место ��охранения ZIP архива.

**Структура архива:**
```
data_split.zip
├── data_1.csv (5 строк + заголовок)
├── data_2.csv (5 строк + заголовок)
├── data_3.csv (5 строк + заголовок)
└── data_4.csv (5 строк + заголовок)
```

---

## 🧪 Тестирование

### Создание тестовых данных

```bash
python test_Data.py
```

Создаст файл `test_data.csv` с 20 строками данных о туристах:
- **Столбцы**: Имя, Город, Тип, Возраст, Статус
- **Особенности**: содержит дубликаты для тестирования функции удаления
- **Кодировка**: UTF-8
- **Разделитель**: точка с запятой (`;`)

### Примеры использования

**Пример 1: Найти всех туристов из Москвы**
```bash
python lbki_csv.py test_data.csv
# Выбрать: 3 (Поиск)
# Ввести: Москва
```

**Пример 2: Оставить только имена и города**
```bash
python lbki_csv.py test_data.csv
# Выбрать: 4 (Выбрать столбцы)
# Ввести: Имя, Город
```

**Пример 3: Удалить дубликаты и разделить на части**
```bash
python lbki_csv.py test_data.csv
# Выбрать: 6 (Удалить дубли)
# Выбрать: 7 (Разделить и ZIP)
# Ввести: 5 строк в части
```

**Пример 4: Использование GUI**
```bash
python lbki_csv_gui.py
# Нажать: 📂 Открыть CSV файл
# Выбрать: test_data.csv
# Выбрать функции из списка
# Нажать: ▶ Запустить выбранное
```

---

## 📁 Структура проекта

```
lbki_csv/
├── lbki_csv.py              # Консольная версия
├── lbki_csv_gui.py          # Графическая версия (GUI)
├── test_Data.py             # Генератор тестовых данных
├── test_data.csv            # Пример CSV файла
├── README.md                # Этот файл
└── .gitignore               # Git конфигурация
```

---

## 🎯 Примеры использования в скриптах

### Использование функций в своём коде

```python
from lbki_csv import detect_encoding, detect_delimiter, read_csv

# Определяем кодировку и разделитель
file_path = "my_data.csv"
encoding = detect_encoding(file_path)
delimiter = detect_delimiter(file_path, encoding)

# Читаем данные
headers, rows = read_csv(file_path, encoding, delimiter)

# Работаем с данными
print(f"Загружено {len(rows)} строк")
for row in rows[:5]:
    print(row)
```

### Удаление дубликатов программно

```python
from lbki_csv import remove_duplicates

headers, rows = read_csv(file_path, encoding, delimiter)
headers, unique_rows = remove_duplicates(headers, rows)
print(f"Осталось {len(unique_rows)} уникальных строк")
```

---

## ⚙️ Поддерживаемые кодировки

| Кодировка | Описание | Регионы |
|-----------|---------|---------|
| **UTF-8** | Универсальная кодировка | Весь мир |
| **CP1251** | Windows Cyrillic | Россия, Украина, Беларусь |

> Инструмент автоматически определяет кодировку при загрузке файла.

---

## 🔍 Поддерживаемые разделители

| Разделитель | Символ | Пример |
|-------------|--------|--------|
| **Запятая** | `,` | `Имя,Город,Возраст` |
| **Точка с запятой** | `;` | `Имя;Город;Возраст` |
| **Табуляция** | `\t` | `Имя	Город	Возраст` |

> Инструмент анализирует первые 1024 символа файла и выбирает наиболее часто встречающийся разделитель.

---

## 🐛 Известные ограничения

1. **Размер файла**: Оптимально для файлов до 100 МБ (зависит от памяти)
2. **Кодировки**: Поддерживаются только UTF-8 и CP1251
3. **Разделители**: Только запятая, точка с запятой и табуляция
4. **GUI**: Требует графический интерфейс (не работает в SSH без X11)
5. **Дубликаты**: Сравнение дубликатов чувствительно к пробелам и регистру

---

## 💡 Советы и трюки

### Быстрый просмотр файла
```bash
python lbki_csv.py data.csv
# Выбрать: 1 (подсчитать)
# Выбрать: 2 (показать первые 10 строк)
```

### Очистка данных
```bash
python lbki_csv.py data.csv
# Выбрать: 6 (удалить дубли)
# Выбрать: 4 (выбрать нужные столбцы)
```

### Конвертация между кодировк��ми
```bash
python lbki_csv.py data_utf8.csv
# Выбрать: 5 (сменить кодировку)
# Ввести: cp1251
```

### Разделение больших файлов
```bash
python lbki_csv.py large_data.csv
# Выбрать: 7 (разделить и ZIP)
# Ввести: 1000 (строк в части)
```

### Использование GUI для быстрых операций
```bash
python lbki_csv_gui.py
# Удобнее для одноразовых операций
# Не требует ввода команд
# Визуальный просмотр результатов
```

---

## 🤝 Вклад и улучшения

Проект открыт для улучшений! Возможные направления:

- [ ] Поддержка дополнительных кодировок (ISO-8859-1, GBK)
- [ ] Фильтрация по условиям (например, возраст > 30)
- [ ] Сортировка по столбцам
- [ ] Объединение нескольких CSV файлов
- [ ] Экспорт в JSON, XML, Excel
- [ ] Параллельная обработка больших файлов
- [ ] Валидация данных
- [ ] Статистический анализ

---

## 📝 Лицензия

Этот проект распространяется свободно. Используйте как угодно!

---

## 📞 Контакты и поддержка

Если у вас есть вопросы или предложения, создайте issue в репозитории.

---

## 🎓 Примеры реальных задач

### Задача 1: Анализ туристических маршрутов
```bash
python lbki_csv.py tourists.csv
# 1. Подсчитать всех туристов
# 3. Найти туристов из Москвы
# 6. Удалить дубликаты
```

**Или через GUI:**
```bash
python lbki_csv_gui.py
# Открыть tourists.csv
# Выбрать функции 1, 3, 6
# Запустить
```

### Задача 2: Подготовка данных для импорта
```bash
python lbki_csv.py raw_data.csv
# 4. Выбрать только нужные столбцы
# 5. Конвертировать в нужную кодировку
# 7. Разделить на части для загрузки
```

### Задача 3: Архивирование больших файлов
```bash
python lbki_csv.py huge_data.csv
# 7. Разделить на части по 5000 строк
# Результат: huge_data_split.zip с частями
```

### Задача 4: Анализ статистики по городам
```bash
python lbki_csv_gui.py
# Открыть data.csv
# Выбрать функцию "Свод по столбцу"
# Выбрать столбец "Город"
# Сохранить результаты в CSV
```

---

## 🏔️ Философия проекта

Как альпинистское снаряжение, этот инструмент:
- **Лёгкий** — без лишних зависимостей
- **Надёжный** — проверен на реальных данных
- **Универсальный** — работает везде, где есть Python
- **Простой** — интуитивный интерфейс
- **Мощный** — решает реальные задачи

---

## 🔄 Сравнение консольной и графической версий

| Функция | Консоль | GUI |
|---------|---------|-----|
| Подсчитать строки | ✅ | ✅ |
| Показать первые N | ✅ | ✅ |
| Поиск | ✅ | ✅ |
| Выбрать столбцы | ✅ | ✅ |
| Смена кодировки | ✅ | ✅ |
| Удалить дубли | ✅ | ✅ |
| Свод по стол��цу | ❌ | ✅ |
| Разделить и ZIP | ✅ | ✅ |
| Интерактивность | Меню | Кнопки |
| Требует ввода | Да | Диалоги |

---

## 📊 Производительность

Тестирование на файле с 100,000 строк:

| Операция | Время |
|----------|-------|
| Загрузка файла | ~0.5 сек |
| Определение кодировки | ~0.1 сек |
| Поиск по всему файлу | ~0.3 сек |
| Удаление дубликатов | ~0.8 сек |
| Разделение на 10 частей | ~0.4 сек |

---

## 🚨 Решение проблем

### Проблема: "Файл не найден"
**Решение:** Убедитесь, что файл находится в текущей директории или укажите полный путь:
```bash
python lbki_csv.py C:\Users\YourName\Documents\data.csv
```

### Проблема: "Не удалось определить кодировку"
**Решение:** Файл может быть повреждён или использовать неподдерживаемую кодировку. Попробуйте открыть его в текстовом редакторе и сохранить в UTF-8.

### Проблема: "Неверный разделитель"
**Решение:** Если разделитель определён неправильно, откройте файл в текстовом редакторе и проверьте, какой символ используется.

### Проблема: GUI не запускается
**Решение:** Убедитесь, что установлен Python с поддержкой tkinter:
```bash
python -m tkinter
```

---

**Версия**: 1.0  
**Последнее обновление**: 2024  
**Статус**: Активно поддерживается ✅
